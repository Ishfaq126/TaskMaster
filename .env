# Copy this to .env and fill in your values
# For local development, use your machine's IP (not localhost) so the device can reach it
EXPO_PUBLIC_API_URL=https://task.digibizz.gob.pk/api
EXPO_ROUTER_APP_ROOT=./app